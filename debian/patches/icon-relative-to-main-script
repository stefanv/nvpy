From: Stefan van der Walt <stefan@sun.ac.za>
Subject: Locate icon relative to main nvpy.py file
Forwarded: no
--- a/nvpy/nvpy.py
+++ b/nvpy/nvpy.py
@@ -189,7 +189,7 @@
             self.appdir, _ = os.path.split(sys.executable)
 
         else:
-            dirname = os.path.dirname(__file__)
+            dirname = os.path.dirname(os.path.realpath(__file__))
             if dirname and dirname != os.curdir:
                 self.appdir = dirname
             else:
